language: node_js
node_js:
  - "8"
  - "node"

services:
  - mongodb

script: npm run lint && npm run test

notifications:
  email:
    on_success: never # default: change
    on_failure: always # default: always
