@Library("pipeline-utils@master")
def PROJECT_NAME = "mongo-distributed-locks"
def EMAIL_TO_NOTIFY = "app@shijigroup.com"

forkNodePipeline(projectName: PROJECT_NAME,
              emailToNotify: EMAIL_TO_NOTIFY)
